# We can also use the range() function to tell Python to skip numbers in a given range
# If you pass the third argument to range(), Python uses that value as a step size
# when generating numbers

even_numbers = list(range(0, 10, 2))
print(even_numbers)